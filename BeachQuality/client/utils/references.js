import { IP_ADDRESS } from "../constants";

export const beachRef = `http://${IP_ADDRESS}:4000/api/beach`;
export const userRef = `http://${IP_ADDRESS}:4000/api/user`;
export const regUserRef = `http://${IP_ADDRESS}:4000/api/user/register`;
export const loginUserRef = `http://${IP_ADDRESS}:4000/api/user/login`;
