
const bname = require("../BeachQuality/index.js");

it('Returns the proper beach', () => {

  expect(BeachQuality.index.data.forEach((item, i) => {
    if(BeachQuality.index.data.indexOf(BeachQuality.index.beachData.name) >= 0 ) {
      return true;
    }
  });
  );
  .catch(function (error) {
    // handle error
    console.log(error);

})
